import React from 'react';

export default function TopBar() {
  return (
    <section className="offer-section dark" style={{ paddingBlock: '12px', textAlign: 'center', fontSize: '14px', borderBottom: '1px solid var(--line-d)' }}>
      <div className="offer-container">
        [[ANNOUNCE]]
      </div>
    </section>
  );
}
